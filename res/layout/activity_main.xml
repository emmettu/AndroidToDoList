<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.todolist.MainActivity" >

    <EditText
        android:id="@+id/addTodoText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:ems="10"
        android:inputType="text"
       	android:hint="@string/input_todo_hint" />

    <Button
        android:id="@+id/addTodoButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/addTodoText"
        android:layout_alignParentLeft="true"
        android:layout_alignTop="@+id/addTodoText"
        android:layout_toLeftOf="@+id/addTodoText"
        android:onClick="addToDo"
        android:text="@string/add_todo_list" />

    <ExpandableListView
        android:id="@+id/addTodoListView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/addTodoButton"
        android:layout_below="@+id/addTodoText" >

    </ExpandableListView>

</RelativeLayout>
